import 'package:flutter/material.dart';

class APPLICATIONSCREEN extends StatelessWidget {
  const APPLICATIONSCREEN({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('panding page'),),
    );
  }
}