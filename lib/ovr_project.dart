import 'package:flutter/material.dart';

class OvrProjectScreen extends StatelessWidget {
  const OvrProjectScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('panding page'),),
    );
  }
}